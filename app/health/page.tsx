import { configurationHealth } from "@/src/config";
export const dynamic="force-dynamic";
export default function Health(){const health=configurationHealth();return <div className="page"><div className="titleRow"><div><div className="eyebrow">CONFIGURAÇÃO LOCAL · SEM CHAMADAS EXTERNAS</div><h1>Health / Readiness</h1></div></div><section className="panel"><p>Esta página valida somente configuração local e nunca exibe segredos.</p><table><thead><tr><th>Componente</th><th>Estado</th><th>Detalhes seguros</th></tr></thead><tbody>{Object.entries(health).map(([name,item])=><tr key={name}><td><b>{name.replaceAll("_"," ")}</b></td><td><span className={item.status==="OK"?"badge good":"badge uncertain"}>{item.status}</span></td><td>{Object.entries(item.details).map(([key,value])=><span className="tag" key={key}>{key}: {String(value)}</span>)}</td></tr>)}</tbody></table></section></div>}
