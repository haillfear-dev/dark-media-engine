import { all,one,Row } from "@/src/db"; import { addIdea } from "@/app/actions"; export const dynamic="force-dynamic";
export default async function Ideas({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;const topic=one<Row>('SELECT * FROM topics WHERE id=?',q.topic||'topic-a')??one<Row>('SELECT * FROM topics ORDER BY editorial_priority_score DESC LIMIT 1')!;const ideas=all<Row>('SELECT i.*,t.title topic_title FROM ideas i JOIN topics t ON t.id=i.topic_id ORDER BY i.created_at DESC');return <div className="page"><div className="titleRow"><div><div className="eyebrow">FATO ≠ ÂNGULO</div><h1>Ideias editoriais</h1></div></div><div className="grid"><section className="panel span5"><h2>Nova ideia a partir do Topic</h2><p><b>{topic.title}</b></p><p>{topic.summary}</p><div className="signals"><span>Viral {topic.virality_score}</span><span>Confiança {topic.confidence_score}</span></div><form action={addIdea} className="formGrid" style={{marginTop:18}}><input type="hidden" name="topicId" value={String(topic.id)}/><label className="full">Título da ideia<input name="title" required/></label><label className="full">Ângulo<textarea name="angle" required/></label><label className="full">Por que este ângulo?<textarea name="rationale"/></label><button className="full">CRIAR E ABRIR NO STUDIO</button></form></section><section className="panel span7"><h2>Banco de ideias</h2><table><thead><tr><th>Ideia</th><th>Topic</th><th>Rank</th><th>Status</th></tr></thead><tbody>{ideas.map(i=><tr key={String(i.id)}><td><b>{i.title}</b><small className="muted" style={{display:'block'}}>{i.angle}</small></td><td>{i.topic_title}</td><td className="score">{i.rank_score}</td><td>{i.status}</td></tr>)}</tbody></table></section></div></div>}
