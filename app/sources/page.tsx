import { all,Row } from "@/src/db"; import { addSource,addSourceItem,toggleSource } from "@/app/actions"; export const dynamic="force-dynamic";
export default function Sources(){const sources=all<Row>('SELECT * FROM sources ORDER BY enabled DESC,priority DESC'),items=all<Row>('SELECT i.*,s.name source_name FROM source_items i JOIN sources s ON s.id=i.source_id ORDER BY discovered_at DESC LIMIT 20');return <div className="page"><div className="titleRow"><div><div className="eyebrow">COLETA E PROVENANCE</div><h1>Fontes</h1></div></div><div className="grid"><section className="panel span7"><h2>Fontes cadastradas</h2><table><thead><tr><th>Fonte</th><th>Tipo</th><th>Prioridade</th><th>Confiabilidade</th><th>Status</th><th/></tr></thead><tbody>{sources.map(s=><tr key={String(s.id)}><td><b>{s.name}</b><small className="muted" style={{display:'block'}}>{s.url}</small></td><td>{s.type}</td><td>{s.priority}</td><td>{s.reliability}</td><td>{s.enabled?'ATIVA':'INATIVA'}</td><td><form action={toggleSource}><input type="hidden" name="id" value={String(s.id)}/><button className="secondary">{s.enabled?'DESATIVAR':'ATIVAR'}</button></form></td></tr>)}</tbody></table></section><section className="panel span5"><h2>Nova fonte manual</h2><form action={addSource} className="formGrid"><input type="hidden" name="brandId" value="brand-radar"/><label>Nome<input name="name" required/></label><label>Tipo<select name="type"><option>WEBSITE</option><option>RSS</option><option>API</option><option>SOCIAL_PROFILE</option><option>TREND_SOURCE</option><option>MANUAL</option><option>SEARCH</option></select></label><label className="full">URL<input name="url" type="url"/></label><label>Prioridade<input name="priority" type="number" min="0" max="100" defaultValue="50"/></label><label>Confiabilidade<input name="reliability" type="number" min="0" max="100" defaultValue="50"/></label><label className="full">Categoria<input name="category" required defaultValue="entretenimento"/></label><button className="full">CADASTRAR FONTE</button></form></section><section className="panel span5"><h2>Inserir Source Item</h2><form action={addSourceItem} className="formGrid"><label className="full">Fonte<select name="sourceId">{sources.map(s=><option value={String(s.id)} key={String(s.id)}>{s.name}</option>)}</select></label><label className="full">Título<input name="title" required/></label><label className="full">Resumo (não copie matéria integral)<textarea name="summary" required/></label><label>URL<input name="url" type="url"/></label><label>Publicação original<input name="publishedAt" type="datetime-local"/></label><label className="full">Grupo de provenance<input name="provenance" placeholder="Origem compartilhada entre republicações"/></label><button className="full">PRESERVAR ITEM</button></form></section><section className="panel span7"><h2>Itens encontrados</h2><table><thead><tr><th>Item</th><th>Origem</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={String(i.id)}><td><b>{i.title}</b><small className="muted" style={{display:'block'}}>{i.summary}</small></td><td>{i.source_name}<small className="muted" style={{display:'block'}}>{i.provenance_group}</small></td><td>{i.processing_status}</td></tr>)}</tbody></table></section></div></div>}
