export type Signals = { conflictScore:number; controversyScore:number; celebrityWeight:number; curiosityScore:number; emotionalIntensity:number; surpriseScore:number; reversalScore:number; commentPotential:number; sharePotential:number; audienceFit:number; freshnessScore:number; momentumScore:number; timingScore:number; saturationRisk:number };
export const VIRALITY_WEIGHTS: Readonly<Record<keyof Signals, number>> = { conflictScore:.08,controversyScore:.07,celebrityWeight:.06,curiosityScore:.09,emotionalIntensity:.07,surpriseScore:.07,reversalScore:.06,commentPotential:.09,sharePotential:.09,audienceFit:.09,freshnessScore:.08,momentumScore:.09,timingScore:.06,saturationRisk:-.06 };
const clamp=(n:number)=>Math.max(0,Math.min(100,Math.round(n)));
export function scoreVirality(signals: Signals){return clamp(Object.entries(VIRALITY_WEIGHTS).reduce((total,[key,weight])=>total+signals[key as keyof Signals]*weight,0));}
export type ConfidenceSignals={primarySource:number; officialConfirmation:number; independentSources:number; reputation:number; consistency:number; documents:number; originalMedia:number; contradictions:number; speculativeLanguage:number; anonymousSources:number; duplicateOriginRisk:number};
export function scoreConfidence(s:ConfidenceSignals){return clamp(s.primarySource*.18+s.officialConfirmation*.18+s.independentSources*.12+s.reputation*.1+s.consistency*.1+s.documents*.09+s.originalMedia*.09-s.contradictions*.06-s.speculativeLanguage*.06-s.anonymousSources*.04-s.duplicateOriginRisk*.04);}
export function scoreEditorialPriority(input:{virality:number;momentum:number;freshness:number;audienceFit:number;confidence:number;saturation:number}){return clamp(input.virality*.32+input.momentum*.22+input.freshness*.18+input.audienceFit*.13+input.confidence*.12-input.saturation*.09);}
export function freshnessFromAge(minutes:number){if(minutes<=30)return "BREAKING";if(minutes<=120)return "VERY_FRESH";if(minutes<=720)return "FRESH";if(minutes<=2880)return "AGING";return "SATURATED";}
